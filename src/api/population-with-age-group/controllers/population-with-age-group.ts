/**
 * population-with-age-group controller
 */

import { factories } from '@strapi/strapi'

export default factories.createCoreController('api::population-with-age-group.population-with-age-group');
